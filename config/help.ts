const helpConfig: Record<string, Record<string, string>> = {
    guide: {
        help: `This command.`,
        wiki: `View the wiki.`
    },
    accounts: {
        link: `Link your account.`,
        unlink: `Unlink your account.`
    },
    stats: {
        stats: `View a user's stats.`,
        update: `Update your roles.`
    }
};

export default helpConfig;
